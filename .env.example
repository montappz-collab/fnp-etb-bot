# Copy to .env for local runs (.env is gitignored). In Railway, set these as
# service Variables instead — never commit real values.
DISCORD_BOT_TOKEN=your-rotated-bot-token-here
TEST_CHANNEL_ID=1507847157686730842
NODE_ENV=production
